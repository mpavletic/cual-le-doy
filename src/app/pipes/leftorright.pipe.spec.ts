import { LeftorrightPipe } from './leftorright.pipe';

describe('LeftorrightPipe', () => {
  it('create an instance', () => {
    const pipe = new LeftorrightPipe();
    expect(pipe).toBeTruthy();
  });
});
